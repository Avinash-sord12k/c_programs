#include <stdio.h>

int main()
{
    // manually creating a linked list
    // struct Node * first;
    // struct Node * second;
    // struct Node * third;
    // struct Node * fourth;

    // first = (struct Node *)malloc(sizeof(struct Node));
    // second = (struct Node *)malloc(sizeof(struct Node));
    // third = (struct Node *)malloc(sizeof(struct Node));
    // fourth = (struct Node *)malloc(sizeof(struct Node));

    // printf("\nSingle node is using %lubytes of space", sizeof(struct Node));

    // first->data = 10;
    // first->next = second;

    // second->data = 20;
    // second->next = third;

    // third->data = 30;
    // third->next = fourth;

    // fourth->data = 40;
    // fourth->next = NULL;

    // traverseLinkedList(first);
}